import { ExceptionHandleFilter } from './exception-handle.filter';

describe('ExceptionFilter', () => {
  it('should be defined', () => {
    expect(new ExceptionHandleFilter()).toBeDefined();
  });
});
